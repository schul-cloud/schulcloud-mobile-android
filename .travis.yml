language: android
android:
  components:
    - platform-tools
    - tools
    - build-tools-25.0.3
    - android-25
    - extra-android-m2repository
    - extra-google-m2repository
    - extra-android-support
    - extra-google-google_play_services

jdk:
  - oraclejdk8

env:
  global:
    ADB_INSTALL_TIMEOUT=6

before_install:
  - travis_scripts/accept_constraintlayout_license_workaround

before_script:
  - travis_scripts/run_emulator

script:
   - ./gradlew assembleDebug testDebugUnitTest connectedDebugAndroidTest